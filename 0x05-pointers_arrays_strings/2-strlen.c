#include "main.h"
/**
*  _strlen - returns the length of a string 
*  @s: string to check
*  Return: 0 is success
*/
int _strlen(char *s)
{
	int i = 0;

    for (i = 0; *s++; i++;)
{
	return (a);
}
}
